# Task

Execute the `implement` skill for issue #{{ISSUE_NUMBER}}: {{ISSUE_TITLE}}.

The skills are mounted read-only at `/home/agent/.pi/agent/skills`. Start by reading
`/home/agent/.pi/agent/skills/engineering/implement/SKILL.md` and follow it exactly. It directs
you to the `tdd` and `code-review` skills, which are mounted alongside it.

Work on the current branch ({{SOURCE_BRANCH}}) and commit as the skill describes.

# Done

When the work is committed and the skill's close-out (including `code-review`) is complete, output:

<promise>COMPLETE</promise>
